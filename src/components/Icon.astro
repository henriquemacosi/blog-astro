---
// src/components/Icon.astro
interface Props {
  name: string;
  class?: string;
}

const { name, class: className } = Astro.props;
---

<svg class={className} aria-hidden="true" fill="currentColor">
  <use xlink:href={`/icons/icons.svg#icon-${name}`}></use>
</svg>